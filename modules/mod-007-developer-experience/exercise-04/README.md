# Error Message Audit — Solution

`ERROR_CATALOG.md` shows 10 worked rewrites: trigger → current → 4-part replacement → where to fix.

Patterns worth keeping:
- Always specify what numerically failed (quota numbers, line numbers)
- Always say where help can be obtained
- Distinguish "server problem" from "client problem" so the user knows whether to retry
