Added a single row to the Shipped table in `CURRICULUM.md:38` for `mod-003-multi-tenancy-resources`, mirroring the existing column shape from the prior mod-001/mod-002 nav-drift entries. Date is today (2026-05-30); the work ID is this nav-drift item (`nav-nav-missing-reference-curriculum-md-mod-003-multi-tenancy-resources`) since `mod-003` has no separate `depth-*` backlog entry in `.aicg/work-plan.json` (the audit reports its status as "ok"). No other sections touched.
