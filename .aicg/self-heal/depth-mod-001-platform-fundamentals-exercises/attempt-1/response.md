Both findings addressed:

- **`missing_required_sections`**: renamed `## 2. Worked answer` → `## 2. Implementation (worked answer)` with a brief framing paragraph explaining that the writeup itself is the implementation. All existing subsections (Analysis template, Michelangelo, Metaflow, Lessons) are preserved as children.
- **`needs_research_marker`**: removed the `<!-- needs-research: ... -->` comment block in §6 References and replaced it with a graders' note explaining that learners must cite the specific resource they read; the source-family bullet list is unchanged.

No other files were touched.
